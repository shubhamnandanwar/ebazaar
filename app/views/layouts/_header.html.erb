<div style = "padding: 0px 20px 0px 20px;">

  <% if current_user %>
    <div style="width:50%; display: inline-block"> 
      <%=link_to "#{current_user.user_name}", profile_path %>
    </div><div id = "header" align = "right" style="width:50%; display: inline-block;">
      <% if current_user.role == "user" %>
        <%= link_to "Cart(#{current_user.cart.cart_items.count})", viewcart_path %>
        &nbsp;&nbsp;
      <% end %>
      <%= link_to "Log out", log_out_path %>
    </div>

  <% else %>
    <div align = "right">
      <%= link_to "Sign up", sign_up_path %> or
      <%= link_to "log in", log_in_path %>
    </div>
  <% end %>
  <hr />
<div>