<tr>
	<td><%= link_to product.name, category_product_path(product.category.id, product.id) %></td>
	<td><%= product.price %></td>
	<td><%= product.description %></td>
	<td><%= link_to 'Edit', edit_category_product_path(product.category.id, product.id) %></td>
	<td><%= link_to 'Destroy', category_product_path(product.category.id, product.id),
              method: :delete,
              data: { confirm: 'Are you sure?' } %></td>
</tr>